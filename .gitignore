# macOS
.DS_Store

# Install/run logs (regenerated by scripts/install.sh & post-install.sh)
*.log

# Personal, machine-specific — never commit these (see *.example templates)
user.env
PROFILE.md
*.local
.env
